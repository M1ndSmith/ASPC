# Local compose only. Anonymous publish is intentional for demos.
# Before any non-local / production use: disable anonymous access, add a
# password_file (or TLS client certs), and prefer an internal Docker network
# without publishing 1883 to the host.
listener 1883
allow_anonymous true
persistence false
